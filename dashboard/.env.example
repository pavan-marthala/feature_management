# API Base URL for the Vue dashboard
# For manual setup, set this to your local API address (e.g., http://localhost:8080)
# For Docker setup, this is handled by build arguments in docker-compose.yml
VITE_API_BASE_URL=http://localhost:8080
